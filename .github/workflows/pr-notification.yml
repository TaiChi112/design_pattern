name: Contributor Welcome Bot
on:
  # ⚠️ ต้องใช้ตัวนี้ เพื่อให้ Bot มีสิทธิ์เขียนคอมเมนต์ใส่ PR ของคนนอก (Fork)
  pull_request_target:
    types: [opened]

permissions:
  pull-requests: write # ขอสิทธิ์เขียนลง PR

jobs:
  welcome:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome Comment
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
          ACTOR: ${{ github.actor }} # คนที่ส่ง PR เข้ามา (Contributor)
          # 👇 แก้ชื่อเพื่อนตรงนี้ครับ (ใส่ @ นำหน้าเพื่อให้แจ้งเตือน)
          OWNERS: "@Narathip1707" 
        run: |
          gh pr comment "$PR_NUMBER" \
            --repo "$REPO" \
            --body "👋 สวัสดีครับคุณ @$ACTOR ! 
            
            ขอบคุณที่มาร่วม Contribute ในโปรเจกต์นี้นะครับ! 🎉
            ระบบได้รับ PR เรียบร้อยแล้ว เดี๋ยวเจ้านายผม $OWNERS จะรีบมาตรวจสอบครับ
            
            (Automated by GitHub Actions 🤖)"