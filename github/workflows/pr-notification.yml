name: PR Notification
on:
  pull_request:
    types: [opened]

permissions:
  pull-requests: write

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Comment and Tag Owner
        if: github.actor != 'friend-username'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_URL: ${{ github.event.pull_request.html_url }}
          OWNER: "@TaiChi112"
        run: |
          gh pr comment "$PR_URL" --body 🚀 มี Pull Request ใหม่เข้ามาครับลูกพี่!

          ผู้ส่ง: @${{ github.actor }}
          ถึง: $OWNER (รบกวนตรวจสอบด้วยครับ)
          (แจ้งเตือนอัตโนมัติโดย GitHub Actions 🤖)
          "